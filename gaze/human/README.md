# Human.js (Vendored)

This directory should contain:

- `human.esm.js` — Main Human.js library (ESM format)
- `models/` — Pre-downloaded model files for offline face detection, face mesh, and iris tracking

## Download

Get Human.js from: https://github.com/vladmandic/human

All models run locally — no network requests needed at runtime.
